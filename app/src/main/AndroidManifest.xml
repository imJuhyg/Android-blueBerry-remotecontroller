<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.limjuhyg.blueberry">
    <!-- Allows applications to connect to paired bluetooth devices. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" /> <!-- Allows applications to discover and pair bluetooth devices. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <!--
 Old permission?
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    -->
    <!-- New bluetooth permission (Android 12) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:name=".applications.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BlueBerry">
        <activity android:name=".views.custom.CustomizeConnectSettingActivity"></activity>
        <activity
            android:name=".views.custom.WidgetContainerActivity"
            android:windowSoftInputMode="adjustNothing"></activity>
        <activity
            android:name=".views.load.LoadActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.custom.CustomizeNameSettingActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".views.testing.BluetoothChatActivity" />
        <activity android:name=".views.scan.DeviceScanActivity" />
        <activity android:name=".views.main.MainActivity" />
    </application>

</manifest>